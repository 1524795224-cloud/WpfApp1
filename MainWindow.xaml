<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="MainWindow" Height="1080" Width="1620"
        WindowStyle="None" ResizeMode="CanResizeWithGrip"
        AllowsTransparency="True">

    <WindowChrome.WindowChrome>
        <WindowChrome NonClientFrameEdges="None" UseAeroCaptionButtons="False" GlassFrameThickness="0"/>
    </WindowChrome.WindowChrome>

    <!-- 窗口资源：定义按钮样式 -->
    <Window.Resources>
        <Style x:Key="CaptionButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="0">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#30FFFFFF"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#50FFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CloseButtonStyle" TargetType="Button" BasedOn="{StaticResource CaptionButtonStyle}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E81123"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#BF0A1A"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <!-- 主窗口背景：深色渐变 -->
    <Window.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#1A1E2B" Offset="0"/>
            <GradientStop Color="#252A3A" Offset="1"/>
        </LinearGradientBrush>
    </Window.Background>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>

        <!-- 标题栏区域 -->
        <Border Grid.Row="0" Background="#2A2F3F" BorderThickness="0,0,0,1" BorderBrush="#3F4555">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 左侧：Logo + 公司名称 -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="20,0,0,0" VerticalAlignment="Center">
                    <Image Source="/Assets/Images/XinYue.ico" Width="42" Height="42"
                           VerticalAlignment="Center" Margin="0,0,15,0"/>
                    <TextBlock Text="宁波市信跃电子科技有限公司"
                               Foreground="#E0E6F0"
                               FontSize="24"
                               FontWeight="SemiBold"
                               VerticalAlignment="Center"
                               FontFamily="Microsoft YaHei UI">
                        <TextBlock.Effect>
                            <DropShadowEffect Color="#ffffffff" BlurRadius="8" ShadowDepth="10" Opacity="0.4" />
                        </TextBlock.Effect>
                    </TextBlock>
                </StackPanel>

                <!-- 右侧：窗口控制按钮 -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Top">
                    <Button  Click="MiniBar"
                            Style="{StaticResource CaptionButtonStyle}"
                            Content="&#xE921;"/>
                    <!-- 最小化图标 -->
                    <Button Click="ScaleWindow"
                            Style="{StaticResource CaptionButtonStyle}"
                            Content="&#xE922;"/>
                    <!-- 最大化图标 -->
                    <Button Click="CloseWindow"
                            Style="{StaticResource CloseButtonStyle}"
                            Content="&#xE106;"/>
                    <!-- 关闭图标 -->
                </StackPanel>
            </Grid>
        </Border>
        <ContentControl Grid.Row="1" Content="{Binding MonitorUC}"></ContentControl>
        <!-- 内容区域（示例：显示连接状态） -->
        <Border Grid.Row="2" Background="#1F2332" BorderThickness="0,1,0,0" BorderBrush="#5518aabd">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition Width="Auto" ></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Border BorderThickness="0,1,0,0" BorderBrush="#5518aabd">
                    <Border.Background>
                        <VisualBrush TileMode="FlipXY" Viewport="0,0,7,7" ViewportUnits="Absolute">
                            <VisualBrush.Visual>
                                <Line X1="0" Y1="10" X2="10" Y2="0" Stroke="White" StrokeThickness="1"/>
                            </VisualBrush.Visual>
                        </VisualBrush>
                    </Border.Background>
                </Border>
                <TextBlock Margin="0,0,15,0" Grid.Column="1" Text="铭诺上位机" Foreground="White" FontWeight="Bold" FontSize="15" >
                    <TextBlock.Effect>
                        <BlurEffect Radius="2"></BlurEffect>
                    </TextBlock.Effect>
                </TextBlock>
            </Grid>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <ContentControl Grid.Column="0" Content="{Binding hightSettingModel}"/>
            <DataGrid Grid.Column="1" Name="LogDataGrid"></DataGrid>

        </Grid>
    </Grid>
</Window>